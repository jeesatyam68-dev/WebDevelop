document.getElementById("action").addEventListener('submit',(e)=>{
    e.preventDefault()  // it stops the default part of the  things, which makes it easy for us to see
    console.log(e.targtet)
    
})

